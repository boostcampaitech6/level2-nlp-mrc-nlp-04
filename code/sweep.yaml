# sweep.yaml
program: sweep.py
method: grid
metric:
  name: eval/exact_match
  goal: maximize
parameters:
  learning_rate:
    values: [5e-05]
  num_train_epochs:
    values: [1,2,3]

  top_k:
    values: [6,15,20,30]


#드롭아웃정도>